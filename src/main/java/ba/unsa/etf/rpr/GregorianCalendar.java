package ba.unsa.etf.rpr;

public interface GregorianCalendar {
    /**
     * Method that checks if a Yer is trespassing
     * @param year integer
     * @return true if year is trespassing
     */
    boolean checkIfTrespassing(int year);
}
